from src.transaktioner.bank_account import BankAccount

class Transaction:
    def __init__(self, transaction_id, name):
        self.id = id
        self.name = name


def transfer(amount, from_account, to_account):
    # Överför pengar från ett konto till ett annat,
    # med hjälp av (bank_account) deposit och withdraw, om det finns pengar.
    pass

